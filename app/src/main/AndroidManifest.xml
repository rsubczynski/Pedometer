<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.radoslawsubczynski.krokomierz">

    <uses-feature android:name="@string/step_detector" android:required="false"/>

    <uses-feature android:name="@string/step_counter" android:required="false"/>

    <application
        android:name="com.example.radoslawsubczynski.krokomierz.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_directions_walk_white_24px"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>